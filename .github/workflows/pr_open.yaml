name: PRopen – Lint, SAST, build snapshot image

on:
  pull_request:
    branches:
      - dev

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/snap-game2048

jobs:
  # ──────────────────────────────────────────
  # Job 1: Detect changed paths
  # Drives conditional execution of downstream jobs:
  #   image → Dockerfile changed (rebuild image)
  #   helm  → helm/** changed (re-lint chart only)
  # ──────────────────────────────────────────
  detect:
    name: Detect Changed Paths
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.filter.outputs.image }}
      helm: ${{ steps.filter.outputs.helm }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Filter changed paths
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            image:
              - 'Dockerfile'
            helm:
              - 'helm/**'

  # ──────────────────────────────────────────
  # Job 2: Helm lint
  # Runs when helm chart or Dockerfile changed
  # ──────────────────────────────────────────
  helm-lint:
    name: Helm Lint
    runs-on: ubuntu-latest
    needs: detect
    if: needs.detect.outputs.image == 'true' || needs.detect.outputs.helm == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Lint chart
        run: helm lint ./helm/game2048

  # ──────────────────────────────────────────
  # Job 3: SAST — Semgrep
  # Only runs when Dockerfile changed (image rebuild needed)
  # Fails on ERROR-severity findings (high confidence)
  # ──────────────────────────────────────────
  sast:
    name: SAST (Semgrep)
    runs-on: ubuntu-latest
    needs: detect
    if: needs.detect.outputs.image == 'true'
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Semgrep
        run: pip install semgrep --quiet

      - name: Run Semgrep SAST (fail on ERROR severity)
        run: semgrep scan --config=auto --error --severity=ERROR --sarif-output=semgrep.sarif .

      - name: Upload SARIF to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        continue-on-error: true  # requires GitHub Advanced Security on private repos
        with:
          sarif_file: semgrep.sarif

  # ──────────────────────────────────────────
  # Job 4: Build and push snapshot image
  # Only runs when Dockerfile changed
  # Tag format: v<appVersion>-pr<number>-g<sha>
  # Image repo: snap-game2048 (distinguishes snapshots from releases)
  # ──────────────────────────────────────────
  build-push:
    name: Build & Push Snapshot Image
    runs-on: ubuntu-latest
    needs: [detect, helm-lint, sast]
    if: needs.detect.outputs.image == 'true'
    permissions:
      contents: read
      packages: write

    outputs:
      full_image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:v${{ steps.ver.outputs.appver }}-pr${{ github.event.pull_request.number }}-g${{ steps.sha.outputs.short }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get short SHA
        id: sha
        run: echo "short=$(git rev-parse --short=7 ${{ github.sha }})" >> "$GITHUB_OUTPUT"

      - name: Get appVersion from Chart.yaml
        id: ver
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
          echo "appver=$(yq '.appVersion' helm/game2048/Chart.yaml | tr -d '\"')" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push snapshot image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:v${{ steps.ver.outputs.appver }}-pr${{ github.event.pull_request.number }}-g${{ steps.sha.outputs.short }}

  # ──────────────────────────────────────────
  # Job 5: Docker image vulnerability scan
  # Only runs when a new image was built
  # Fails on HIGH or CRITICAL fixable CVEs
  # ──────────────────────────────────────────
  security-scan:
    name: Image Vulnerability Scan (Trivy)
    runs-on: ubuntu-latest
    needs: [detect, build-push]
    if: needs.detect.outputs.image == 'true'
    permissions:
      contents: read
      packages: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Trivy (table output for visibility)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ needs.build-push.outputs.full_image }}
          format: table
          exit-code: 0
          severity: HIGH,CRITICAL
          ignore-unfixed: true
        env:
          TRIVY_USERNAME: ${{ github.actor }}
          TRIVY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}

      - name: Run Trivy (fail on HIGH or CRITICAL with available fix)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ needs.build-push.outputs.full_image }}
          format: sarif
          output: trivy.sarif
          exit-code: 1
          severity: HIGH,CRITICAL
          ignore-unfixed: true
        env:
          TRIVY_USERNAME: ${{ github.actor }}
          TRIVY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Trivy SARIF to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        continue-on-error: true  # requires GitHub Advanced Security on private repos
        with:
          sarif_file: trivy.sarif
